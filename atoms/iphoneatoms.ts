import { atom } from 'recoil';
export const iPhoneState:any = atom({
    key: 'iphonestate', // unique ID (with respect to other atoms/selectors)
    default: {
    }, // default value (aka initial value)
})


export const MacState:any = atom({
    key: 'MacState', // unique ID (with respect to other atoms/selectors)
    default: {
    }, // default value (aka initial value)
})
export const AirpodsState:any = atom({
    key: 'AirpodsState', // unique ID (with respect to other atoms/selectors)
    default: {
    }, // default value (aka initial value)
})
export const WatchState:any = atom({
    key: 'WatchState', // unique ID (with respect to other atoms/selectors)
    default: {
    }, // default value (aka initial value)
})
export const iPadState:any = atom({
    key: 'iPadState', // unique ID (with respect to other atoms/selectors)
    default: {
    }, // default value (aka initial value)
})

